[project]
name = "junk-drawer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.1",
    "waitress>=3.0.2",
]

[dependency-groups]
dev = [
    "ruff>=0.14.7",
]
